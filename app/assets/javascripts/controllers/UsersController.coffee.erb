controllers = angular.module('controllers')
controllers.controller("UsersController", [ '$scope', '$routeParams', '$location', 'User',
  ($scope,$routeParams,$location,User)->
    $scope.search = (keywords) -> $scope.users = User.list.query(keywords: keywords)
   
    if $routeParams.keywords? > 0 
      params = { keywords: $routeParams }
    else
      params = {}
     
    User.list.query(params, (results)-> $scope.users = results)
    $scope.view = (userId)-> $location.url("/users/#{userId}")
])
